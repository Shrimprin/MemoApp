<div class="mb-3">
  <a class="btn btn-primary" href="/memos/new">新規作成</a>
</div>

<ul>
  <% @memos.each do |memo| %>
    <li><a href="/memos/<%= memo['id'] %>"><%= CGI.escapeHTML(memo['title']) %></a></li>
  <% end %>
</ul>
